repeat task.wait() until game:IsLoaded()

local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()
local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer
local Camera = workspace.CurrentCamera
local RunService = game:GetService("RunService")
local Workspace = game:GetService("Workspace")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local VirtualInputManager = game:GetService("VirtualInputManager")

getgenv().x1y2z3 = false
getgenv().a2b3c4 = 20
getgenv().d5e6f7 = false
getgenv().g8h9i0 = 50
getgenv().j1k2l3 = false

local fevberwindow1 = Rayfield:CreateWindow({
    Name = "FEVBER | 99 Nights in the Forest",
    Icon = 0,
    LoadingTitle = "FEVBER Loading...",
    LoadingSubtitle = "by FEVBER Team",
    ConfigurationSaving = {
        Enabled = true,
        FolderName = "FEVBER",
        FileName = "99NitF"
    },
    Discord = {
        Enabled = true,
        Invite = "dsc.gg/fevber",
        RememberJoins = true
    },
    KeySystem = false
})

local fevbertab1 = fevberwindow1:CreateTab("Main", 0)
local fevbertab2 = fevberwindow1:CreateTab("Teleport", 0)
local fevbertab3 = fevberwindow1:CreateTab("Player", 0)
local fevbertab4 = fevberwindow1:CreateTab("ESP", 0)
local fevbertab5 = fevberwindow1:CreateTab("Bring All", 0)
local fevbertab6 = fevberwindow1:CreateTab("Bring General", 0)
local fevbertab7 = fevberwindow1:CreateTab("Bring Weapon", 0)
local fevbertab8 = fevberwindow1:CreateTab("Bring Items", 0)
local fevbertab9 = fevberwindow1:CreateTab("Bring Food", 0)
local fevbertab10 = fevberwindow1:CreateTab("Hitbox", 0)
local fevbertab11 = fevberwindow1:CreateTab("Misc", 0)

local function m1n2o3p4()
    local root = LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
    if not root then return end
    for _, item in ipairs(workspace.Items:GetChildren()) do
        if item.Name:lower():find("log") and item:IsA("Model") then
            local main = item:FindFirstChildWhichIsA("BasePart")
            if main then
                main.CFrame = root.CFrame * CFrame.new(math.random(-5,5), 0, math.random(-5,5))
            end
        end
    end
end

local function q5r6s7t8(name)
    local root = LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
    if not root then return end
    for _, item in ipairs(workspace.Items:GetChildren()) do
        if item.Name:lower():find(name:lower()) then
            local part = item:FindFirstChildWhichIsA("BasePart") or (item:IsA("BasePart") and item)
            if part then
                part.CFrame = root.CFrame + Vector3.new(0, 3, 0) + root.CFrame.LookVector * 5
            end
        end
    end
end

local function u9v0w1x2(Char, Color, Text, Parent, numberOffset)
    if not Char or not Char:IsA("Model") then return end
    if not Char:FindFirstChild("HumanoidRootPart") then return end
    if not Parent or not Parent:IsA("BasePart") then return end
    
    local highlight = Char:FindFirstChildOfClass("Highlight")
    if highlight then highlight:Destroy() end
    local billboard = Parent:FindFirstChildOfClass("BillboardGui")
    if billboard then billboard:Destroy() end

    highlight = Instance.new("Highlight")
    highlight.Name = "ESP_Highlight"
    highlight.Adornee = Char
    highlight.FillColor = Color
    highlight.FillTransparency = 1
    highlight.OutlineColor = Color
    highlight.OutlineTransparency = 0
    highlight.DepthMode = Enum.HighlightDepthMode.AlwaysOnTop
    highlight.Enabled = true
    highlight.Parent = Char

    billboard = Instance.new("BillboardGui")
    billboard.Name = "ESP"
    billboard.Size = UDim2.new(0, 50, 0, 25)
    billboard.AlwaysOnTop = true
    billboard.StudsOffset = Vector3.new(0, numberOffset or 3, 0)
    billboard.Adornee = Parent
    billboard.Enabled = true
    billboard.Parent = Parent

    local label = Instance.new("TextLabel")
    label.Size = UDim2.new(1, 0, 1, 0)
    label.BackgroundTransparency = 1
    label.Text = Text
    label.TextColor3 = Color
    label.TextScaled = true
    label.Parent = billboard

    task.spawn(function()
        while highlight and billboard and Parent and Parent.Parent do
            local cameraPosition = Camera and Camera.CFrame.Position
            if cameraPosition and Parent:IsA("BasePart") then
                local distance = (cameraPosition - Parent.Position).Magnitude
                if getgenv().y3z4a5b6 then
                    label.Text = Text .. " (" .. math.floor(distance + 0.5) .. " stud)"
                else
                    label.Text = Text
                end
            end
            RunService.Heartbeat:Wait()
        end
    end)
end

local function c7d8e9f0()
    for _, item in ipairs(workspace.Items:GetChildren()) do
        local part = item:FindFirstChildWhichIsA("BasePart") or item:IsA("BasePart") and item
        if part then
            part.CFrame = LocalPlayer.Character.HumanoidRootPart.CFrame + Vector3.new(math.random(-25,25), 0, math.random(-25,25))
        end
    end
end

local g1h2i3j4 = false
local k5l6m7n8 = nil

local function o9p0q1r2()
    local ReplicatedStorage = game:GetService("ReplicatedStorage")
    local RequestConsumeItem = ReplicatedStorage:WaitForChild("RemoteEvents"):WaitForChild("RequestConsumeItem")
    while g1h2i3j4 do
        local args = { Instance.new("Model", nil) }
        RequestConsumeItem:InvokeServer(unpack(args))
        task.wait(1)
    end
end

local s3t4u5v6 = false
local w7x8y9z0 = nil

local function a1b2c3d4()
    local ReplicatedStorage = game:GetService("ReplicatedStorage")
    local ToolDamageObject = ReplicatedStorage:WaitForChild("RemoteEvents"):WaitForChild("ToolDamageObject")
    local Backpack = LocalPlayer:WaitForChild("Backpack")
    
    local function e5f6g7h8()
        local map = workspace:FindFirstChild("Map")
        if not map then return {} end
        local landmarks = map:FindFirstChild("Landmarks") or map:FindFirstChild("Foliage")
        if not landmarks then return {} end
        local trees = {}
        for _, tree in ipairs(landmarks:GetChildren()) do
            if tree.Name == "Small Tree" and tree:IsA("Model") and tree.Parent then
                local trunk = tree:FindFirstChild("Trunk") or tree.PrimaryPart
                if trunk then
                    table.insert(trees, {tree = tree, trunk = trunk})
                end
            end
        end
        return trees
    end
    
    local function i9j0k1l2()
        local inv = LocalPlayer:FindFirstChild("Inventory")
        if not inv then return nil end
        return inv:FindFirstChild("Old Axe") or inv:FindFirstChildWhichIsA("Tool")
    end
    
    while s3t4u5v6 do
        local trees = e5f6g7h8()
        for _, t in ipairs(trees) do
            if not s3t4u5v6 then break end
            if t.tree and t.tree.Parent then
                local char = LocalPlayer.Character or LocalPlayer.CharacterAdded:Wait()
                local hrp = char:FindFirstChild("HumanoidRootPart", 3)
                if hrp and t.trunk then
                    local treeCFrame = t.trunk.CFrame
                    local rightVector = treeCFrame.RightVector
                    local targetPosition = treeCFrame.Position + rightVector * 3
                    hrp.CFrame = CFrame.new(targetPosition)
                    task.wait(0.25)
                    local axe = i9j0k1l2()
                    if axe then
                        if axe.Parent == Backpack then
                            axe.Parent = char
                            task.wait(0.15)
                        end
                        while t.tree.Parent and s3t4u5v6 do
                            pcall(function() axe:Activate() end)
                            local args = { t.tree, axe, "1_8264699301", t.trunk.CFrame }
                            pcall(function() ToolDamageObject:InvokeServer(unpack(args)) end)
                            task.wait(1)
                        end
                    end
                end
            end
            task.wait(0.5)
        end
        task.wait(1)
    end
end

local m3n4o5p6 = false
local q7r8s9t0 = nil
local u1v2w3x4 = 1

local function y5z6a7b8()
    local player = game.Players.LocalPlayer
    local camera = workspace.CurrentCamera
    while m3n4o5p6 do
        local function c9d0e1f2()
            local inv = player:FindFirstChild("Inventory")
            return inv and (inv:FindFirstChild("Spear") or inv:FindFirstChild("Strong Axe") or inv:FindFirstChild("Good Axe") or inv:FindFirstChild("Old Axe"))
        end
        local weapon = c9d0e1f2()
        if weapon then
            local ray = workspace:Raycast(camera.CFrame.Position, camera.CFrame.LookVector * 15)
            if ray and ray.Instance and ray.Instance.Name == "Trunk" then
                game:GetService("ReplicatedStorage").RemoteEvents.ToolDamageObject:InvokeServer(ray.Instance.Parent, weapon, "4_7591937906", CFrame.new(ray.Position))
            end
        end
        task.wait(u1v2w3x4)
    end
end

local g3h4i5j6 = false
local k7l8m9n0 = false
local o1p2q3r4 = false
local s5t6u7v8 = false
local w9x0y1z2 = false

local a3b4c5d6 = {
    Wolf = false, Bunny = false, Cultist = false, Alien = false, Bear = false, All = false, Show = false, Size = 10
}

local function e7f8g9h0(model)
    local root = model:FindFirstChild("HumanoidRootPart")
    if not root then return end
    local name = model.Name:lower()
    local i1j2k3l4 = (a3b4c5d6.All and (name:find("alien") or name:find("alien elite") or name:find("bear") or name:find("polar bear") or name:find("wolf") or name:find("alpha") or name:find("bunny") or name:find("dragon") or name:find("cultist") or name:find("cross") or name:find("crossbow cultist"))) or (a3b4c5d6.Alien and (name:find("alien") or name:find("alien elite"))) or (a3b4c5d6.Wolf and (name:find("wolf") or name:find("alpha"))) or (a3b4c5d6.Bunny and name:find("bunny")) or (a3b4c5d6.Bear and (name:find("bear") or name:find("polar bear"))) or (a3b4c5d6.Cultist and (name:find("cultist") or name:find("cross") or name:find("crossbow cultist")))
    if i1j2k3l4 then
        root.Size = Vector3.new(a3b4c5d6.Size, a3b4c5d6.Size, a3b4c5d6.Size)
        root.Transparency = a3b4c5d6.Show and 0.5 or 1
        root.Color = Color3.fromRGB(255, 255, 255)
        root.Material = Enum.Material.Neon
        root.CanCollide = false
    end
end

task.spawn(function()
    while true do
        for _, model in ipairs(workspace:GetDescendants()) do
            if model:IsA("Model") and model:FindFirstChild("HumanoidRootPart") then
                e7f8g9h0(model)
            end
        end
        task.wait(2)
    end
end)

local m5n6o7p8 = nil

Rayfield:Notify({
    Title = "FEVBER Loaded!",
    Content = "99 Nights in the Forest | dsc.gg/fevber",
    Duration = 5
})

fevbertab1:CreateToggle({
    Name = "Inf Hunger",
    CurrentValue = false,
    Flag = "infhunger",
    Callback = function(v)
        g1h2i3j4 = v
        if v then
            k5l6m7n8 = task.spawn(o9p0q1r2)
        else
            if k5l6m7n8 then task.cancel(k5l6m7n8) end
        end
    end
})

fevbertab1:CreateButton({
    Name = "Auto Cook Meat",
    Callback = function()
        local campfirePos = Vector3.new(1.87, 4.33, -3.67)
        for _, item in pairs(workspace.Items:GetChildren()) do
            if item:IsA("Model") or item:IsA("BasePart") then
                local name = item.Name:lower()
                if name:find("meat") then
                    local part = item:FindFirstChildWhichIsA("BasePart") or item
                    if part then
                        part.CFrame = CFrame.new(campfirePos + Vector3.new(math.random(-2,2), 0.5, math.random(-2,2)))
                    end
                end
            end
        end
    end
})

fevbertab1:CreateToggle({
    Name = "Auto Tree Farm",
    CurrentValue = false,
    Flag = "autotree",
    Callback = function(v)
        s3t4u5v6 = v
        if v then
            w7x8y9z0 = task.spawn(a1b2c3d4)
        else
            if w7x8y9z0 then task.cancel(w7x8y9z0) end
        end
    end
})

fevbertab1:CreateSlider({
    Name = "Auto Hit Speed",
    Range = {0.1, 2},
    Increment = 0.1,
    Suffix = "s",
    CurrentValue = 1,
    Flag = "autohitspeed",
    Callback = function(v)
        u1v2w3x4 = v
    end
})

fevbertab1:CreateToggle({
    Name = "Auto Hit Break",
    CurrentValue = false,
    Flag = "autohit",
    Callback = function(v)
        m3n4o5p6 = v
        if v then
            q7r8s9t0 = task.spawn(y5z6a7b8)
        else
            if q7r8s9t0 then task.cancel(q7r8s9t0) end
        end
    end
})

fevbertab4:CreateToggle({
    Name = "ESP Player",
    CurrentValue = false,
    Flag = "espplayer",
    Callback = function(v)
        g3h4i5j6 = v
        task.spawn(function()
            while g3h4i5j6 do
                for _, player in pairs(game.Players:GetPlayers()) do
                    if player ~= LocalPlayer and player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
                        local char = player.Character
                        if not char:FindFirstChildOfClass("Highlight") then
                            u9v0w1x2(char, Color3.fromRGB(0, 255, 0), player.Name, char.HumanoidRootPart, 2)
                        end
                    end
                end
                task.wait(0.1)
            end
        end)
    end
})

fevbertab4:CreateToggle({
    Name = "ESP Items",
    CurrentValue = false,
    Flag = "espitems",
    Callback = function(v)
        k7l8m9n0 = v
        task.spawn(function()
            while k7l8m9n0 do
                for _, Obj in pairs(game.Workspace.Items:GetChildren()) do
                    if Obj:IsA("Model") and Obj.PrimaryPart and not Obj:FindFirstChildOfClass("Highlight") then
                        u9v0w1x2(Obj, Color3.fromRGB(255, 255, 0), Obj.Name, Obj.PrimaryPart, 2)
                    end
                end
                task.wait(0.1)
            end
        end)
    end
})

fevbertab4:CreateToggle({
    Name = "ESP Enemies",
    CurrentValue = false,
    Flag = "espenemies",
    Callback = function(v)
        o1p2q3r4 = v
        task.spawn(function()
            while o1p2q3r4 do
                for _, Obj in pairs(game.Workspace.Characters:GetChildren()) do
                    if Obj:IsA("Model") and Obj.PrimaryPart and (Obj.Name ~= "Lost Child" and Obj.Name ~= "Lost Child2" and Obj.Name ~= "Lost Child3" and Obj.Name ~= "Lost Child4" and Obj.Name ~= "Pelt Trader") and not Obj:FindFirstChildOfClass("Highlight") then
                        u9v0w1x2(Obj, Color3.fromRGB(255, 0, 0), Obj.Name, Obj.PrimaryPart, 2)
                    end
                end
                task.wait(0.1)
            end
        end)
    end
})

fevbertab4:CreateToggle({
    Name = "ESP Children",
    CurrentValue = false,
    Flag = "espchildren",
    Callback = function(v)
        s5t6u7v8 = v
        task.spawn(function()
            while s5t6u7v8 do
                for _, Obj in pairs(game.Workspace.Characters:GetChildren()) do
                    if Obj:IsA("Model") and Obj.PrimaryPart and (Obj.Name == "Lost Child" or Obj.Name == "Lost Child2" or Obj.Name == "Lost Child3" or Obj.Name == "Lost Child4") and not Obj:FindFirstChildOfClass("Highlight") then
                        u9v0w1x2(Obj, Color3.fromRGB(0, 255, 0), Obj.Name, Obj.PrimaryPart, 2)
                    end
                end
                task.wait(0.1)
            end
        end)
    end
})

fevbertab4:CreateToggle({
    Name = "ESP Pelt Trader",
    CurrentValue = false,
    Flag = "esppelt",
    Callback = function(v)
        w9x0y1z2 = v
        task.spawn(function()
            while w9x0y1z2 do
                for _, Obj in pairs(game.Workspace.Characters:GetChildren()) do
                    if Obj:IsA("Model") and Obj.PrimaryPart and Obj.Name == "Pelt Trader" and not Obj:FindFirstChildOfClass("Highlight") then
                        u9v0w1x2(Obj, Color3.fromRGB(0, 255, 255), Obj.Name, Obj.PrimaryPart, 2)
                    end
                end
                task.wait(0.1)
            end
        end)
    end
})

fevbertab2:CreateButton({
    Name = "Teleport to Camp",
    Callback = function()
        local char = LocalPlayer.Character
        if char and char:FindFirstChild("HumanoidRootPart") then
            char.HumanoidRootPart.CFrame = CFrame.new(13.287363052368164, 3.999999761581421, 0.36212217807769775)
        end
    end
})

fevbertab2:CreateButton({
    Name = "TP to NPC Trader",
    Callback = function()
        local character = LocalPlayer.Character or LocalPlayer.CharacterAdded:Wait()
        local hrp = character:WaitForChild("HumanoidRootPart")
        hrp.CFrame = CFrame.new(-37.08, 3.98, -16.33)
    end
})

fevbertab2:CreateButton({
    Name = "TP to Random Tree",
    Callback = function()
        local character = LocalPlayer.Character or LocalPlayer.CharacterAdded:Wait()
        local hrp = character:FindFirstChild("HumanoidRootPart", 3)
        if not hrp then return end
        local map = workspace:FindFirstChild("Map")
        if not map then return end
        local foliage = map:FindFirstChild("Foliage") or map:FindFirstChild("Landmarks")
        if not foliage then return end
        local trees = {}
        for _, obj in ipairs(foliage:GetChildren()) do
            if obj.Name == "Small Tree" and obj:IsA("Model") then
                local trunk = obj:FindFirstChild("Trunk") or obj.PrimaryPart
                if trunk then table.insert(trees, trunk) end
            end
        end
        if #trees > 0 then
            local trunk = trees[math.random(1, #trees)]
            local targetPosition = trunk.Position + trunk.CFrame.RightVector * 3
            hrp.CFrame = CFrame.new(targetPosition)
        end
    end
})

for i = 1, 4 do
    fevbertab2:CreateButton({
        Name = "TP to Lost Child " .. i,
        Callback = function()
            local name = "Lost Child" .. (i > 1 and i or "")
            local target = game.Workspace.Characters:FindFirstChild(name)
            if target and target:IsA("Model") and target.PrimaryPart then
                local hrp = (LocalPlayer.Character or LocalPlayer.CharacterAdded:Wait()):WaitForChild("HumanoidRootPart")
                hrp.CFrame = target.PrimaryPart.CFrame
            end
        end
    })
end

fevbertab2:CreateToggle({
    Name = "Scan Map",
    CurrentValue = false,
    Flag = "scanmap",
    Callback = function(v)
        getgenv().j1k2l3 = v
        task.spawn(function()
            local character = LocalPlayer.Character or LocalPlayer.CharacterAdded:Wait()
            local hrp = character:WaitForChild("HumanoidRootPart", 3)
            if not hrp then return end
            local map = workspace:FindFirstChild("Map")
            if not map then return end
            local foliage = map:FindFirstChild("Foliage") or map:FindFirstChild("Landmarks")
            if not foliage then return end
            local trees = {}
            for _, obj in ipairs(foliage:GetChildren()) do
                if obj.Name == "Small Tree" and obj:IsA("Model") then
                    local trunk = obj:FindFirstChild("Trunk") or obj.PrimaryPart
                    if trunk then table.insert(trees, trunk) end
                end
            end
            while getgenv().j1k2l3 and #trees > 0 do
                for _, trunk in ipairs(trees) do
                    if not getgenv().j1k2l3 then break end
                    local targetPosition = trunk.Position + trunk.CFrame.RightVector * 25
                    hrp.CFrame = CFrame.new(targetPosition)
                    task.wait(0.15)
                end
            end
        end)
    end
})

fevbertab5:CreateButton({
    Name = "Bring Everything",
    Callback = c7d8e9f0
})

fevbertab5:CreateButton({
    Name = "Bring Logs All",
    Callback = m1n2o3p4
})

fevbertab5:CreateButton({
    Name = "Bring Scrap All",
    Callback = function()
        local root = LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
        if not root then return end
        local scrapNames = { tyre = true, ["Old Car Engine"] = true, ["Washing Machine"] = true, ["UFO Component"] = true, ["Cultist Prototype"] = true, ["sheet metal"] = true, ["broken fan"] = true, bolt = true, ["old radio"] = true, ["ufo junk"] = true, ["ufo scrap"] = true, ["broken microwave"] = true }
        for _, item in pairs(workspace.Items:GetChildren()) do
            if item:IsA("Model") then
                local itemName = item.Name:lower()
                for scrapName, _ in pairs(scrapNames) do
                    if itemName:find(scrapName) then
                        local main = item:FindFirstChildWhichIsA("BasePart")
                        if main then
                            main.CFrame = root.CFrame * CFrame.new(math.random(-15,15), 0, math.random(-15,15))
                        end
                        break
                    end
                end
            end
        end
    end
})

fevbertab5:CreateButton({
    Name = "Bring Lost Children All",
    Callback = function()
        local root = LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
        for _, item in pairs(workspace.Characters:GetChildren()) do
            local name = item.Name:lower()
            if (name:find("lost child")) and item:IsA("Model") then
                local main = item:FindFirstChildWhichIsA("BasePart")
                if main then
                    main.CFrame = root.CFrame * CFrame.new(math.random(-5,5), 0, math.random(-5,5))
                end
            end
        end
    end
})

fevbertab7:CreateButton({ Name = "Bring Morningstar", Callback = function() q5r6s7t8("Morningstar") end })
fevbertab7:CreateButton({ Name = "Bring Laser Sword", Callback = function() q5r6s7t8("Laser Sword") end })
fevbertab7:CreateButton({ Name = "Bring Katana", Callback = function() q5r6s7t8("Katana") end })
fevbertab7:CreateButton({ Name = "Bring Spear", Callback = function() q5r6s7t8("Spear") end })
fevbertab7:CreateButton({ Name = "Bring Tactical Shotgun", Callback = function() q5r6s7t8("Tactical Shotgun") end })
fevbertab7:CreateButton({ Name = "Bring Kunai", Callback = function() q5r6s7t8("Kunai") end })
fevbertab7:CreateButton({ Name = "Bring Knife", Callback = function() q5r6s7t8("Knife") end })
fevbertab7:CreateButton({ Name = "Bring Revolver", Callback = function() q5r6s7t8("Revolver") end })
fevbertab7:CreateButton({ Name = "Bring Rifle", Callback = function() q5r6s7t8("Rifle") end })
fevbertab7:CreateButton({ Name = "Bring Alien Raygun", Callback = function() q5r6s7t8("Ray Gun") end })
fevbertab7:CreateButton({ Name = "Bring Alien Raycannon", Callback = function() q5r6s7t8("Laser Cannon") end })
fevbertab7:CreateButton({ Name = "Bring Strong Axe", Callback = function() q5r6s7t8("Strong Axe") end })
fevbertab7:CreateButton({ Name = "Bring Good Axe", Callback = function() q5r6s7t8("Good Axe") end })
fevbertab7:CreateButton({ Name = "Bring Old Axe", Callback = function() q5r6s7t8("Old Axe") end })
fevbertab7:CreateButton({ Name = "Bring Rifle Ammo", Callback = function() q5r6s7t8("Rifle Ammo") end })
fevbertab7:CreateButton({ Name = "Bring Revolver Ammo", Callback = function() q5r6s7t8("Revolver Ammo") end })
fevbertab7:CreateButton({ Name = "Bring Leather Body", Callback = function() q5r6s7t8("Leather Body") end })
fevbertab7:CreateButton({ Name = "Bring Iron Body", Callback = function() q5r6s7t8("Iron Body") end })
fevbertab7:CreateButton({ Name = "Bring Thorn Body", Callback = function() q5r6s7t8("Thorn Body") end })
fevbertab7:CreateButton({ Name = "Bring Alien Armor", Callback = function() q5r6s7t8("Alien Armor") end })
fevbertab7:CreateButton({ Name = "Bring Riot Shield", Callback = function() q5r6s7t8("Riot Shield") end })

fevbertab9:CreateButton({ Name = "Bring Steak", Callback = function() q5r6s7t8("Steak") end })
fevbertab9:CreateButton({ Name = "Bring Apple", Callback = function() q5r6s7t8("Apple") end })
fevbertab9:CreateButton({ Name = "Bring Berry", Callback = function() q5r6s7t8("Berry") end })
fevbertab9:CreateButton({ Name = "Bring Hearty Stew", Callback = function() q5r6s7t8("Hearty Stew") end })
fevbertab9:CreateButton({ Name = "Bring Stew", Callback = function() q5r6s7t8("Stew") end })
fevbertab9:CreateButton({ Name = "Bring Chili", Callback = function() q5r6s7t8("Chili") end })
fevbertab9:CreateButton({ Name = "Bring Pumpkin", Callback = function() q5r6s7t8("Pumpkin") end })
fevbertab9:CreateButton({ Name = "Bring Corn", Callback = function() q5r6s7t8("Corn") end })
fevbertab9:CreateButton({ Name = "Bring Carrot", Callback = function() q5r6s7t8("Carrot") end })
fevbertab9:CreateButton({ Name = "Bring Cake", Callback = function() q5r6s7t8("Cake") end })
fevbertab9:CreateButton({ Name = "Bring Morsel", Callback = function() q5r6s7t8("Morsel") end })
fevbertab9:CreateButton({ Name = "Bring Meat Sandwich", Callback = function() q5r6s7t8("Meat? Sandwich") end })

fevbertab8:CreateButton({ Name = "Bring Rope", Callback = function() q5r6s7t8("Rope") end })
fevbertab8:CreateButton({ Name = "Bring Scrap", Callback = function() q5r6s7t8("Scrap") end })
fevbertab8:CreateButton({ Name = "Bring Wood", Callback = function() q5r6s7t8("Wood") end })
fevbertab8:CreateButton({ Name = "Bring Rock", Callback = function() q5r6s7t8("Rock") end })
fevbertab8:CreateButton({ Name = "Bring Bolt", Callback = function() q5r6s7t8("Bolt") end })
fevbertab8:CreateButton({ Name = "Bring Sheet Metal", Callback = function() q5r6s7t8("Sheet Metal") end })
fevbertab8:CreateButton({ Name = "Bring Cloth", Callback = function() q5r6s7t8("Cloth") end })
fevbertab8:CreateButton({ Name = "Bring Tyre", Callback = function() q5r6s7t8("Tyre") end })
fevbertab8:CreateButton({ Name = "Bring Nails", Callback = function() q5r6s7t8("Nails") end })
fevbertab8:CreateButton({ Name = "Bring Stone Pickaxe", Callback = function() q5r6s7t8("Stone Pickaxe") end })
fevbertab8:CreateButton({ Name = "Bring Fan", Callback = function() q5r6s7t8("Fan") end })
fevbertab8:CreateButton({ Name = "Bring Broken Fan", Callback = function() q5r6s7t8("Broken Fan") end })
fevbertab8:CreateButton({ Name = "Bring Broken Microwave", Callback = function() q5r6s7t8("Broken Microwave") end })
fevbertab8:CreateButton({ Name = "Bring UFO Junk", Callback = function() q5r6s7t8("UFO Junk") end })
fevbertab8:CreateButton({ Name = "Bring UFO Scrap", Callback = function() q5r6s7t8("UFO Scrap") end })
fevbertab8:CreateButton({ Name = "Bring Seed Box", Callback = function() q5r6s7t8("Seed Box") end })
fevbertab8:CreateButton({ Name = "Bring Coin Stack", Callback = function() q5r6s7t8("Coin Stack") end })
fevbertab8:CreateButton({ Name = "Bring Old Radio", Callback = function() q5r6s7t8("Old Radio") end })
fevbertab8:CreateButton({ Name = "Bring Chair", Callback = function() q5r6s7t8("Chair") end })

fevbertab6:CreateButton({ Name = "Bring Chili Seeds", Callback = function() q5r6s7t8("Chili Seeds") end })
fevbertab6:CreateButton({ Name = "Bring Firefly Seeds", Callback = function() q5r6s7t8("Firefly Seeds") end })
fevbertab6:CreateButton({ Name = "Bring Berry Seeds", Callback = function() q5r6s7t8("Berry Seeds") end })
fevbertab6:CreateButton({ Name = "Bring Flower Seeds", Callback = function() q5r6s7t8("Flower Seeds") end })
fevbertab6:CreateButton({ Name = "Bring Old Sack", Callback = function() q5r6s7t8("Old Sack") end })
fevbertab6:CreateButton({ Name = "Bring Good Sack", Callback = function() q5r6s7t8("Good Sack") end })
fevbertab6:CreateButton({ Name = "Bring Giant Sack", Callback = function() q5r6s7t8("Giant Sack") end })
fevbertab6:CreateButton({ Name = "Bring Flashlight", Callback = function() q5r6s7t8("Flashlight") end })
fevbertab6:CreateButton({ Name = "Bring Strong Flashlight", Callback = function() q5r6s7t8("Strong Flashlight") end })
fevbertab6:CreateButton({ Name = "Bring Bandage", Callback = function() q5r6s7t8("Bandage") end })
fevbertab6:CreateButton({ Name = "Bring MedKit", Callback = function() q5r6s7t8("MedKit") end })
fevbertab6:CreateButton({ Name = "Bring Saplings", Callback = function() q5r6s7t8("Saplings") end })
fevbertab6:CreateButton({ Name = "Bring Stakes", Callback = function() q5r6s7t8("Stakes") end })

fevbertab10:CreateToggle({
    Name = "Expand All Hitbox",
    CurrentValue = false,
    Flag = "hitboxall",
    Callback = function(v) a3b4c5d6.All = v end
})

fevbertab10:CreateToggle({
    Name = "Expand Alien Hitbox",
    CurrentValue = false,
    Flag = "hitboxalien",
    Callback = function(v) a3b4c5d6.Alien = v end
})

fevbertab10:CreateToggle({
    Name = "Expand Bear Hitbox",
    CurrentValue = false,
    Flag = "hitboxbear",
    Callback = function(v) a3b4c5d6.Bear = v end
})

fevbertab10:CreateToggle({
    Name = "Expand Wolf Hitbox",
    CurrentValue = false,
    Flag = "hitboxwolf",
    Callback = function(v) a3b4c5d6.Wolf = v end
})

fevbertab10:CreateToggle({
    Name = "Expand Bunny Hitbox",
    CurrentValue = false,
    Flag = "hitboxbunny",
    Callback = function(v) a3b4c5d6.Bunny = v end
})

fevbertab10:CreateToggle({
    Name = "Expand Cultist Hitbox",
    CurrentValue = false,
    Flag = "hitboxcultist",
    Callback = function(v) a3b4c5d6.Cultist = v end
})

fevbertab10:CreateSlider({
    Name = "Hitbox Size",
    Range = {2, 200},
    Increment = 1,
    Suffix = "studs",
    CurrentValue = 10,
    Flag = "hitboxsize",
    Callback = function(v) a3b4c5d6.Size = v end
})

fevbertab10:CreateToggle({
    Name = "Show Hitbox",
    CurrentValue = false,
    Flag = "hitboxshow",
    Callback = function(v) a3b4c5d6.Show = v end
})

fevbertab3:CreateSlider({
    Name = "WalkSpeed",
    Range = {5, 500},
    Increment = 1,
    Suffix = "studs/s",
    CurrentValue = 16,
    Flag = "walkspeed",
    Callback = function(v)
        local hum = LocalPlayer.Character and LocalPlayer.Character:FindFirstChildOfClass("Humanoid")
        if hum then hum.WalkSpeed = v end
    end
})

fevbertab3:CreateSlider({
    Name = "JumpPower",
    Range = {10, 500},
    Increment = 1,
    Suffix = "",
    CurrentValue = 50,
    Flag = "jumppower",
    Callback = function(v)
        local hum = LocalPlayer.Character and LocalPlayer.Character:FindFirstChildOfClass("Humanoid")
        if hum then hum.JumpPower = v end
    end
})

fevbertab3:CreateButton({
    Name = "Fly",
    Callback = function()
        loadstring(game:HttpGet("https://raw.githubusercontent.com/dyumra/dyumrascript-/refs/heads/main/Flua"))()
    end
})

local noclipconn = nil
fevbertab3:CreateToggle({
    Name = "No Clip",
    CurrentValue = false,
    Flag = "noclip",
    Callback = function(v)
        if v then
            noclipconn = RunService.Stepped:Connect(function()
                local Character = LocalPlayer.Character
                if Character then
                    for _, part in pairs(Character:GetDescendants()) do
                        if part:IsA("BasePart") then
                            part.CanCollide = false
                        end
                    end
                end
            end)
        else
            if noclipconn then noclipconn:Disconnect() end
            local Character = LocalPlayer.Character
            if Character then
                for _, part in pairs(Character:GetDescendants()) do
                    if part:IsA("BasePart") then
                        part.CanCollide = true
                    end
                end
            end
        end
    end
})

fevbertab3:CreateToggle({
    Name = "Infinity Jump",
    CurrentValue = false,
    Flag = "infjump",
    Callback = function(v)
        local hum = LocalPlayer.Character and LocalPlayer.Character:FindFirstChildOfClass("Humanoid")
        if hum then
            if v then
                hum.JumpPower = math.huge
                local conn
                conn = hum.StateChanged:Connect(function(old, new)
                    if new == Enum.HumanoidStateType.Landed and hum.JumpPower == math.huge then
                        hum:ChangeState(Enum.HumanoidStateType.Jumping)
                    end
                end)
                hum:SetAttribute("InfJumpConn", conn)
            else
                hum.JumpPower = 50
                local conn = hum:GetAttribute("InfJumpConn")
                if conn then conn:Disconnect() end
            end
        end
    end
})

fevbertab11:CreateButton({
    Name = "FPS Boost",
    Callback = function()
        pcall(function()
            settings().Rendering.QualityLevel = Enum.QualityLevel.Level01
            local lighting = game:GetService("Lighting")
            lighting.Brightness = 0
            lighting.FogEnd = 100
            lighting.GlobalShadows = false
            lighting.EnvironmentDiffuseScale = 0
            lighting.EnvironmentSpecularScale = 0
            lighting.ClockTime = 14
            lighting.OutdoorAmbient = Color3.new(0, 0, 0)
            local terrain = workspace:FindFirstChildOfClass("Terrain")
            if terrain then
                terrain.WaterWaveSize = 0
                terrain.WaterWaveSpeed = 0
                terrain.WaterReflectance = 0
                terrain.WaterTransparency = 1
            end
            for _, obj in ipairs(lighting:GetDescendants()) do
                if obj:IsA("PostEffect") or obj:IsA("BloomEffect") or obj:IsA("ColorCorrectionEffect") or obj:IsA("SunRaysEffect") or obj:IsA("BlurEffect") then
                    obj.Enabled = false
                end
            end
            for _, obj in ipairs(game:GetDescendants()) do
                if obj:IsA("ParticleEmitter") or obj:IsA("Trail") then
                    obj.Enabled = false
                elseif obj:IsA("Texture") or obj:IsA("Decal") then
                    obj.Transparency = 1
                end
            end
            for _, part in ipairs(workspace:GetDescendants()) do
                if part:IsA("BasePart") then
                    part.CastShadow = false
                end
            end
        end)
    end
})

local showFPS, showPing = true, true
local fpsText, msText = Drawing.new("Text"), Drawing.new("Text")
fpsText.Size = 16
fpsText.Position = Vector2.new(Camera.ViewportSize.X - 100, 10)
fpsText.Color = Color3.fromRGB(0, 255, 0)
fpsText.Center = false
fpsText.Outline = true
fpsText.Visible = showFPS

msText.Size = 16
msText.Position = Vector2.new(Camera.ViewportSize.X - 100, 30)
msText.Color = Color3.fromRGB(0, 255, 0)
msText.Center = false
msText.Outline = true
msText.Visible = showPing

local fpsCounter, fpsLastUpdate = 0, tick()

RunService.RenderStepped:Connect(function()
    fpsCounter = fpsCounter + 1
    if tick() - fpsLastUpdate >= 1 then
        if showFPS then
            fpsText.Text = "FPS: " .. tostring(fpsCounter)
            fpsText.Visible = true
        else
            fpsText.Visible = false
        end
        if showPing then
            local pingStat = game:GetService("Stats").Network.ServerStatsItem["Data Ping"]
            local ping = pingStat and math.floor(pingStat:GetValue()) or 0
            msText.Text = "Ping: " .. ping .. " ms"
            if ping <= 60 then
                msText.Color = Color3.fromRGB(0, 255, 0)
            elseif ping <= 120 then
                msText.Color = Color3.fromRGB(255, 165, 0)
            else
                msText.Color = Color3.fromRGB(255, 0, 0)
            end
            msText.Visible = true
        else
            msText.Visible = false
        end
        fpsCounter = 0
        fpsLastUpdate = tick()
    end
end)

fevbertab11:CreateToggle({
    Name = "Show FPS",
    CurrentValue = true,
    Flag = "showfps",
    Callback = function(v) showFPS = v end
})

fevbertab11:CreateToggle({
    Name = "Show Ping",
    CurrentValue = true,
    Flag = "showping",
    Callback = function(v) showPing = v end
})